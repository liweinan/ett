<% if @package.status && @package.status.code == 'inprogress' && !@package.task.readonly? %>
    <tr>
        <td>Change files in RPM repo</td>
        <td><%= render :partial => 'packages/fields/maven_build_arguments' %>
            <%= render :partial => 'packages/fields/edit_ini_file' %>
            <%= render :partial => 'packages/fields/spec_file' %></td>
    </tr>
<% end %>
