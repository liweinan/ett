<span style="text-align: left;">
  <% if logged_in? && !@package.task.read_only_task? %>
      <button onclick="TINY.box.show({url:'/toolbox/press_build_button?id=<%=@package.id%>'})">Submit Build</button>
  <% end %>
</span>
