<% if params[:brew_tag_id].blank?
    params[:brew_tag_id] = @package.brew_tag.name
end %>
<tr>
  <td>
    Label
  </td>
  <td style="text-align:left;">
    <%= render :partial => "labels/selection_for_tag", :locals => { :tag_id => BrewTag.find_by_name(unescape_url(params[:brew_tag_id])).id, :form => f }%>
    <%= render :partial => 'sync', :locals => {:key => 'label'} %>
  </td>
</tr>
