<% if params[:brew_tag_id].blank?
    params[:brew_tag_id] = @package.brew_tag.name
end %>
<tr>
  <td>
    Label
  </td>
  <td style="text-align:left;">
    <%= f.collection_select :label_id, Label.find_all_can_select_by_brew_tag_id_in_global_scope(BrewTag.find_by_name(unescape_url(params[:brew_tag_id])).id), :id, :name, {:include_blank => " - "} %>
    <%= render :partial => 'sync', :locals => {:key => 'label'} %>
  </td>
</tr>
