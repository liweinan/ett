<% package.reload %>
<% package.bz_bugs.each do |bz| %>
    <% if bz != package.upgrade_bz %>
        <%=link_to bz.bz_id, "#{APP_CONFIG['bugzilla_prefix']}#{bz.bz_id}", :target => '_blank' %>
    <% end %>
<% end %>
