<% disabled ||= nil %>
<% if field_name == 'Ver' && !package.can_edit_version?
     disabled = true
   end %>

<%= text_field_tag "package[#{field_name.underscore}]", package[field_name.underscore], :id => "pac_#{package.id}_#{field_name}_input", :disabled => disabled %>