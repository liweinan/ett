<% is_active = task.active? ? " [CRONJOB_ACTIVE]": " [FROZEN]" %>
<%= link_to "#{task.name + is_active}", :controller => :packages, :action => :index, :task_id => escape_url(task.name), :user => user %>
/
