<h1>Listing p_attachments</h1>

<table>
  <tr>
  </tr>

  <% @p_attachments.each do |p_attachment| %>
      <tr>
        <td><%= link_to 'Show', p_attachment %></td>
        <td><%= link_to 'Edit', edit_p_attachment_path(p_attachment) %></td>
        <td><%= link_to 'Destroy', p_attachment, :confirm => 'Are you sure?', :method => :delete %></td>
      </tr>
  <% end %>
</table>

<br/>

<%= link_to 'New p_attachment', new_p_attachment_path %>